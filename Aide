:/\n\n\nNote:/
:/^[^ ]+$/
Dump /github/credo/dump
Edit ,s,\n\n\nNote: ,\n\nNote: ,g
Edit ,|s ''
Edit ,|sort|uniq
Load /github/credo/dump
cat sh-inferno/* | grep -v '^[	 }{]*$' | grep -v '^[	 ]*#' |  wc -l
dos git add doc lib man sh-inferno test
dos git commit -a -m 'Add crelay to walk down redep graph and print leaf nodes, to list the minimal set of targets that would need rebuilt if a file changes.'
dos git diff
dos git log --decorate --graph --oneline --all | sed 10q
dos git status
du -n | grep -v '\.git' | grep Aide
du -n | grep -v '\.git' | grep sh-inferno
du | grep -v '\.git'
fortune doc/dictionary | fmt
git
gn env sh-inferno/*
log
log git
log git commit -a -m 'Cache and clean up checksum manipulation.  Add past test results.'
log git | grep -v commit
ls -F | mc
