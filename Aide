% git pull --rebase
:/\n\n\nNote:/
:/^[^ ]+$/
Dump /github/credo/dump
Edit ,s,\n\n\nNote: ,\n\nNote: ,g
Edit ,|s ''
Edit ,|sort|uniq
Load /github/credo/dump
cat sh-inferno/* | grep -v '^[	 }{]*$' | grep -v '^[	 ]*#' |  wc -l
du -n | grep -v '\.git' | grep Aide
du -n | grep -v '\.git' | grep sh-inferno
du | grep -v '\.git'
fortune doc/dictionary | fmt
git
git commit -a -m 'New top-level wrapper "cre" binds $credir and invokes scripts in /dis/cresh.  Fix $osdir since git repos now lie in /usr/coinferno.'
gn env sh-inferno/*
host gedit scratch
git add .
git log --decorate --graph --oneline --all | sed 10q
git log --decorate --oneline | sed 10q
log
log 1 git | grep add
log git
log git | grep -v commit
ls -F | mc
whatis host
win
