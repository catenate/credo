% git pull --rebase
:/\n\n\nNote:/
:/^[^ ]+$/
Dump /github/credo/dump
Edit ,s,\n\n\nNote: ,\n\nNote: ,g
Edit ,|s ''
Edit ,|sort|uniq
Load /github/credo/dump
cat sh-inferno/* | grep -v '^[	 }{]*$' | grep -v '^[	 ]*#' |  wc -l
du -n | grep -v '\.git' | grep Aide
du -n | grep -v '\.git' | grep sh-inferno
du | grep -v '\.git'
fortune doc/dictionary | fmt
git
git commit -a -m 'Add a bash version of pastout, and the log command.  Plan to have every interesting credo command log its options to credo.log, for a reviewable list of previously-executed credo commands.'
gn env sh-inferno/*
host gedit scratch
host git add .
host git commit -a -m 'Update todo and todont.'
host git diff
host git log --decorate --graph --oneline --all | sed 10q
host git status
log
log 1 git | grep add
log git
log git | grep -v commit
ls -F | mc
whatis host
win
