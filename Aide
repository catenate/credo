:/\n\n\nNote:/
:/^[^ ]+$/
Dump /github/credo/dump
Edit ,s,\n\n\nNote: ,\n\nNote: ,g
Edit ,|s ''
Edit ,|sort|uniq
Load /github/credo/dump
du -n | grep -v '\.git' | grep Aide
du -n | grep -v '\.git' | grep sh-inferno
du | grep -v '\.git'
fortune doc/dictionary | fmt
git
git log --decorate --graph --oneline --all | sed 10q
gn env sh-inferno/*
log
log git
log git commit -a -m 'Add deduce to find (and optionally execute) a libdo command for a target which uses available source files.  Add C/C++ do scripts from boilermaker makefile, using -MM and -MF to generate header-file dependencies.  Test deduce with boilermaker C/C++ examples, and a hello example.  Clean up lscatdo output to ignore dep productions.'
log git | grep -v commit
ls -F | mc
