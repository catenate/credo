% git pull --rebase
:/\n\n\nNote:/
:/^[^ ]+$/
Dump /github/credo/dump
Edit ,s,\n\n\nNote: ,\n\nNote: ,g
Edit ,|s ''
Edit ,|sort|uniq
Load /github/credo/dump
cat sh-inferno/* | grep -v '^[	 }{]*$' | grep -v '^[	 ]*#' |  wc -l
du -n | grep -v '\.git' | grep Aide
du -n | grep -v '\.git' | grep sh-inferno
du | grep -v '\.git'
fortune doc/dictionary | fmt
git
git commit -a -m 'Sfix implements namelists (see /doc/mk.pdf), to replace prefixes and suffixes around %.  Currently doesn''t allow % as a replaced or replacement character in the patterns.  Update lex and yacc do scripts to use sfix.  Calc test program just segfaults on Ubuntu 12.10.'
gn env sh-inferno/*
host gedit scratch
host git add .
host git log --decorate --graph --oneline --all | sed 10q
log
log 1 git | grep add
log git
log git | grep -v commit
ls -F | mc
whatis host
win
